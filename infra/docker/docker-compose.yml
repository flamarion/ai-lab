services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - web-ui
      - llm-gateway
    restart: unless-stopped

  llm-gateway:
    build:
      context: ../../
      dockerfile: services/llm-gateway/Dockerfile
    expose:
      - "8000"
    env_file:
      - ../../.env
    environment:
      - PYTHONPATH=/app:/app/shared/python
      - DATABASE_URL=${DATABASE_URL:-postgresql://ailab:ailab_dev@192.168.1.202:5432/ailab}
    restart: unless-stopped

  web-ui:
    build:
      context: ../../apps/web-ui
      dockerfile: Dockerfile
    expose:
      - "3000"
    depends_on:
      - llm-gateway
    restart: unless-stopped

  # Legacy Streamlit UI — kept for reference, not started by default
  # chat-ui:
  #   build:
  #     context: ../../apps/chat-ui
  #     dockerfile: Dockerfile
  #   expose:
  #     - "8501"
  #   environment:
  #     - GATEWAY_URL=http://llm-gateway:8000
  #   depends_on:
  #     - llm-gateway
  #   restart: unless-stopped
